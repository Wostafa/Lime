import Editor from '../components/editor/editor'
import {screen , render, waitFor} from '@testing-library/react'

test('title is self controlled', async()=>{
  // const con = jest.spyOn(console, 'log')
  // const p = render(<Editor/>)
  // screen.debug(v);
  // render(<Editor/>);
  // let v = await screen.findAllByRole('button', undefined, {timeout: 5000})
  // screen.debug(v)
  // expect(con).toBeCalledWith("Editor.js 2.25.0 I'm ready! (ﾉ◕ヮ◕)ﾉ*:･ﾟ✧")
})